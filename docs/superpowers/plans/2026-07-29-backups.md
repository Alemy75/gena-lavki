# Backups & Restore Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать проекту ручную систему бэкапов БД + загрузок (копии на VPS и на Mac) и восстановления, без каких-либо доп. сервисов.

**Architecture:** Пять коротких shell-скриптов + Makefile. Серверные `backup.sh`/`restore.sh` работают через существующий Docker-стек и доезжают на сервер деплоем. Mac-скрипты (`pull-backup.sh`, `pull-env.sh`, `push-restore.sh`) по SSH запускают серверные и качают/заливают бандлы. Скрипты параметризованы, поэтому цикл backup→restore тестируется локально на dev-стеке (`docker-compose.yml`).

**Tech Stack:** bash, Docker Compose, PostgreSQL 16 (`pg_dump`/`psql`), tar/gzip, ssh/scp, GNU make.

Полный дизайн: [2026-07-29-backups-design.md](../specs/2026-07-29-backups-design.md).

## Global Constraints

- **bash 3.2-совместимость** (штатный bash macOS): без `mapfile`, без `xargs -r`. Ротация — через `... | while IFS= read -r f; do rm -f "$f"; done`.
- Скрипты вызываются как `bash script.sh` → бит `+x` не обязателен, `chmod` в деплое не нужен.
- Серверный рабочий каталог — `/opt/gena-lavki`, прод-compose — `docker-compose.prod.yml`. Оба переопределяются через `WORKDIR` / `COMPOSE_FILE`.
- Подключение к БД — по локальному сокету внутри контейнера `db`, пароль берётся из уже присутствующей переменной `POSTGRES_PASSWORD` (не завязываемся на `pg_hba`). Пользователь/БД — `catalog`/`catalog`.
- **`.env` (секреты) НИКОГДА не попадает в бандлы бэкапа.** Он бэкапится отдельно `pull-env.sh`.
- Формат дампа — `pg_dump --clean --if-exists` (plain SQL) → `gzip`. Не custom-формат.
- Имя бандла: `gena-lavki_YYYY-MM-DD_HHMMSS.tgz`; внутри — `db.sql.gz`, `uploads.tgz`, `manifest.txt`.
- Ротация: сервер — `KEEP=14`, Mac — `KEEP=30`.
- Никакого cron/автоматики и никаких облачных хранилищ (по требованию пользователя).

---

### Task 1: `backup.sh` — сборка бандла (+ .gitignore)

**Files:**
- Create: `catalog/scripts/backup.sh`
- Modify: `catalog/.gitignore`

**Interfaces:**
- Consumes: Docker-стек (сервисы `db`, `app`) через `COMPOSE_FILE`.
- Produces: файл `"$BACKUP_DIR/gena-lavki_<ts>.tgz"`, содержащий `db.sql.gz`, `uploads.tgz`, `manifest.txt`. Печатает `OK: <path>`. Env-параметры: `WORKDIR` (def `/opt/gena-lavki`), `COMPOSE_FILE` (def `docker-compose.prod.yml`), `BACKUP_DIR` (def `$WORKDIR/backups`), `KEEP` (def 14).

- [ ] **Step 1: Поднять dev-стек и создать маркеры (setup для теста)**

Run:
```bash
cd catalog
docker compose up -d --build
# дождаться готовности БД
until docker compose exec -T db pg_isready -U catalog -d catalog >/dev/null 2>&1; do sleep 2; done
# маркер в БД (своя таблица, схема Prisma не нужна)
docker compose exec -T db psql -U catalog -d catalog -c \
  "CREATE TABLE IF NOT EXISTS backup_smoke(x text); TRUNCATE backup_smoke; INSERT INTO backup_smoke VALUES ('marker42');"
# маркер в загрузках
docker compose exec -T app sh -c 'echo hi > /app/public/uploads/marker.txt'
```
Expected: команды завершаются без ошибок.

- [ ] **Step 2: Тест — запустить будущий скрипт, убедиться что падает (его ещё нет)**

Run:
```bash
cd catalog
WORKDIR="$PWD" COMPOSE_FILE=docker-compose.yml BACKUP_DIR="$PWD/backups" bash scripts/backup.sh
```
Expected: FAIL — `bash: scripts/backup.sh: No such file or directory`.

- [ ] **Step 3: Написать `catalog/scripts/backup.sh`**

```bash
#!/usr/bin/env bash
# Создаёт бэкап БД + загрузок одним .tgz и чистит старые.
# Запуск НА сервере: bash /opt/gena-lavki/scripts/backup.sh
# Локально против dev-стека:
#   WORKDIR="$PWD" COMPOSE_FILE=docker-compose.yml BACKUP_DIR="$PWD/backups" bash scripts/backup.sh
set -euo pipefail

WORKDIR="${WORKDIR:-/opt/gena-lavki}"
COMPOSE_FILE="${COMPOSE_FILE:-docker-compose.prod.yml}"
BACKUP_DIR="${BACKUP_DIR:-$WORKDIR/backups}"
KEEP="${KEEP:-14}"

cd "$WORKDIR"
C=(docker compose -f "$COMPOSE_FILE")

ts="$(date +%Y-%m-%d_%H%M%S)"
work="$(mktemp -d)"
trap 'rm -rf "$work"' EXIT

# 1) Дамп БД (пароль из окружения контейнера, коннект по локальному сокету)
"${C[@]}" exec -T db sh -c 'PGPASSWORD="$POSTGRES_PASSWORD" pg_dump -U catalog -d catalog --clean --if-exists' \
  | gzip > "$work/db.sql.gz"

# 2) Загрузки (том catalog_uploads смонтирован в app:/app/public/uploads)
"${C[@]}" exec -T app tar -czf - -C /app/public/uploads . > "$work/uploads.tgz"

# 3) Манифест
{
  echo "created: $ts"
  echo "compose_file: $COMPOSE_FILE"
  echo "app_image: $("${C[@]}" images -q app 2>/dev/null || true)"
  "${C[@]}" exec -T db postgres --version 2>/dev/null || true
} > "$work/manifest.txt"

# 4) Единый бандл
mkdir -p "$BACKUP_DIR"
bundle="$BACKUP_DIR/gena-lavki_${ts}.tgz"
tar -czf "$bundle" -C "$work" db.sql.gz uploads.tgz manifest.txt

# 5) Ротация: оставить KEEP самых свежих (bash 3.2-safe, без xargs -r)
ls -1t "$BACKUP_DIR"/gena-lavki_*.tgz 2>/dev/null | tail -n +$((KEEP + 1)) \
  | while IFS= read -r f; do rm -f "$f"; done

echo "OK: $bundle"
```

- [ ] **Step 4: Тест — запустить и проверить содержимое бандла**

Run:
```bash
cd catalog
WORKDIR="$PWD" COMPOSE_FILE=docker-compose.yml BACKUP_DIR="$PWD/backups" bash scripts/backup.sh
b=$(ls -1t backups/gena-lavki_*.tgz | head -1)
tar tzf "$b"
tmp=$(mktemp -d); tar xzf "$b" -C "$tmp"
gunzip -c "$tmp/db.sql.gz" | grep -c marker42
tar tzf "$tmp/uploads.tgz" | grep marker.txt
```
Expected: `tar tzf` показывает `db.sql.gz`, `uploads.tgz`, `manifest.txt`; grep по `marker42` печатает `1`; в `uploads.tgz` есть `./marker.txt`.

- [ ] **Step 5: Игнорировать локальную папку бэкапов в git**

Add to `catalog/.gitignore`:
```
# локальные бэкапы (тесты и ручные прогоны)
/backups/
```

- [ ] **Step 6: Commit**

```bash
git add scripts/backup.sh .gitignore
git commit -m "feat(ops): add backup.sh (db + uploads bundle with rotation)"
```

---

### Task 2: `restore.sh` — восстановление из бандла

**Files:**
- Create: `catalog/scripts/restore.sh`

**Interfaces:**
- Consumes: бандл из Task 1 (`gena-lavki_<ts>.tgz` c `db.sql.gz`/`uploads.tgz`), тот же Docker-стек через `COMPOSE_FILE`.
- Produces: восстановленное состояние БД и загрузок. Env: `WORKDIR`, `COMPOSE_FILE`, `FORCE` (`1` = не спрашивать подтверждение). Аргумент `$1` = путь к бандлу.

- [ ] **Step 1: Тест — испортить данные, запустить будущий restore (его ещё нет)**

Run (dev-стек из Task 1 всё ещё поднят):
```bash
cd catalog
b=$(ls -1t backups/gena-lavki_*.tgz | head -1)
# ломаем: удаляем маркеры
docker compose exec -T db psql -U catalog -d catalog -c "DROP TABLE IF EXISTS backup_smoke;"
docker compose exec -T app sh -c 'rm -f /app/public/uploads/marker.txt'
FORCE=1 WORKDIR="$PWD" COMPOSE_FILE=docker-compose.yml bash scripts/restore.sh "$b"
```
Expected: FAIL — `bash: scripts/restore.sh: No such file or directory`.

- [ ] **Step 2: Написать `catalog/scripts/restore.sh`**

```bash
#!/usr/bin/env bash
# Восстанавливает БД + загрузки из бандла .tgz. ПЕРЕЗАПИСЫВАЕТ текущие данные.
# Запуск НА сервере: bash /opt/gena-lavki/scripts/restore.sh /opt/gena-lavki/backups/<файл>.tgz
# Локально: FORCE=1 WORKDIR="$PWD" COMPOSE_FILE=docker-compose.yml bash scripts/restore.sh backups/<файл>.tgz
set -euo pipefail

WORKDIR="${WORKDIR:-/opt/gena-lavki}"
COMPOSE_FILE="${COMPOSE_FILE:-docker-compose.prod.yml}"
bundle="${1:?укажи путь к бандлу .tgz}"
[ -f "$bundle" ] || { echo "нет файла: $bundle" >&2; exit 1; }
# абсолютный путь до распаковки cd
case "$bundle" in /*) : ;; *) bundle="$PWD/$bundle" ;; esac

cd "$WORKDIR"
C=(docker compose -f "$COMPOSE_FILE")

if [ "${FORCE:-0}" != 1 ]; then
  printf 'Восстановить из %s? Текущие данные будут перезаписаны. [y/N] ' "$bundle"
  read -r ok
  [ "$ok" = y ] || { echo "отменено"; exit 1; }
fi

work="$(mktemp -d)"
trap 'rm -rf "$work"' EXIT
tar -xzf "$bundle" -C "$work"

# 1) БД (ON_ERROR_STOP — падать на первой ошибке SQL)
gunzip -c "$work/db.sql.gz" \
  | "${C[@]}" exec -T db sh -c 'PGPASSWORD="$POSTGRES_PASSWORD" psql -U catalog -d catalog -v ON_ERROR_STOP=1'

# 2) Загрузки: очистить и распаковать заново
"${C[@]}" exec -T app sh -c 'rm -rf /app/public/uploads/* && mkdir -p /app/public/uploads && tar -xzf - -C /app/public/uploads' \
  < "$work/uploads.tgz"

echo "Восстановлено из $bundle"
```

- [ ] **Step 3: Тест — запустить restore и проверить, что маркеры вернулись**

Run:
```bash
cd catalog
b=$(ls -1t backups/gena-lavki_*.tgz | head -1)
docker compose exec -T db psql -U catalog -d catalog -c "DROP TABLE IF EXISTS backup_smoke;"
docker compose exec -T app sh -c 'rm -f /app/public/uploads/marker.txt'
FORCE=1 WORKDIR="$PWD" COMPOSE_FILE=docker-compose.yml bash scripts/restore.sh "$b"
docker compose exec -T db psql -U catalog -d catalog -tAc "SELECT x FROM backup_smoke;"
docker compose exec -T app sh -c 'cat /app/public/uploads/marker.txt'
```
Expected: `psql` печатает `marker42`; `cat` печатает `hi`.

- [ ] **Step 4: Убрать за собой тестовый маркер и (опционально) погасить стек**

Run:
```bash
cd catalog
docker compose exec -T db psql -U catalog -d catalog -c "DROP TABLE IF EXISTS backup_smoke;"
docker compose exec -T app sh -c 'rm -f /app/public/uploads/marker.txt'
# стек можно оставить поднятым или: docker compose down
```
Expected: без ошибок.

- [ ] **Step 5: Commit**

```bash
git add scripts/restore.sh
git commit -m "feat(ops): add restore.sh (db + uploads from bundle)"
```

---

### Task 3: Mac-скрипты — `pull-backup.sh`, `pull-env.sh`, `push-restore.sh`

**Files:**
- Create: `catalog/scripts/pull-backup.sh`
- Create: `catalog/scripts/pull-env.sh`
- Create: `catalog/scripts/push-restore.sh`

**Interfaces:**
- Consumes: серверные `scripts/backup.sh`/`scripts/restore.sh` в `$REMOTE_DIR/scripts/` (появятся на сервере в Task 5), SSH-доступ `REMOTE`.
- Produces: `pull-backup.sh` кладёт свежий бандл в `$LOCAL_DIR` (def `~/gena-lavki-backups`); `pull-env.sh` кладёт `.env` в `$SECRETS_DIR` (def `~/gena-lavki-backups/secrets`, `chmod 600`); `push-restore.sh` заливает локальный бандл (`$1`) на сервер и запускает restore. Общие env: `REMOTE` (def `root@201.51.4.231`), `REMOTE_DIR` (def `/opt/gena-lavki`).

> Полный e2e этих скриптов требует живого сервера, поэтому в плане — статическая проверка (`bash -n` + `shellcheck`, если установлен). Реальный прогон `make pull` — в приёмке после первого деплоя со скриптами (Task 5).

- [ ] **Step 1: Тест — синтаксическая проверка будущих скриптов (падает, файлов нет)**

Run:
```bash
cd catalog
bash -n scripts/pull-backup.sh
```
Expected: FAIL — `No such file or directory`.

- [ ] **Step 2: Написать `catalog/scripts/pull-backup.sh`**

```bash
#!/usr/bin/env bash
# Mac: делает свежий бэкап на сервере и скачивает его сюда.
set -euo pipefail
REMOTE="${REMOTE:-root@201.51.4.231}"
REMOTE_DIR="${REMOTE_DIR:-/opt/gena-lavki}"
LOCAL_DIR="${LOCAL_DIR:-$HOME/gena-lavki-backups}"
KEEP="${KEEP:-30}"

mkdir -p "$LOCAL_DIR"
ssh "$REMOTE" "bash $REMOTE_DIR/scripts/backup.sh"
newest="$(ssh "$REMOTE" "ls -1t $REMOTE_DIR/backups/gena-lavki_*.tgz | head -1")"
[ -n "$newest" ] || { echo "на сервере нет бандлов" >&2; exit 1; }
scp "$REMOTE:$newest" "$LOCAL_DIR/"
ls -1t "$LOCAL_DIR"/gena-lavki_*.tgz 2>/dev/null | tail -n +$((KEEP + 1)) \
  | while IFS= read -r f; do rm -f "$f"; done
echo "Скачано: $LOCAL_DIR/$(basename "$newest")"
```

- [ ] **Step 3: Написать `catalog/scripts/pull-env.sh`**

```bash
#!/usr/bin/env bash
# Mac: разово скачать серверный .env в приватную папку. Запускать при смене секретов.
set -euo pipefail
REMOTE="${REMOTE:-root@201.51.4.231}"
REMOTE_DIR="${REMOTE_DIR:-/opt/gena-lavki}"
SECRETS_DIR="${SECRETS_DIR:-$HOME/gena-lavki-backups/secrets}"

mkdir -p "$SECRETS_DIR"
chmod 700 "$SECRETS_DIR"
scp "$REMOTE:$REMOTE_DIR/.env" "$SECRETS_DIR/.env"
chmod 600 "$SECRETS_DIR/.env"
echo "Сохранено: $SECRETS_DIR/.env (запускай снова только при смене секретов)"
```

- [ ] **Step 4: Написать `catalog/scripts/push-restore.sh`**

```bash
#!/usr/bin/env bash
# Mac: залить локальный бандл на сервер и восстановить из него (DR).
set -euo pipefail
REMOTE="${REMOTE:-root@201.51.4.231}"
REMOTE_DIR="${REMOTE_DIR:-/opt/gena-lavki}"
bundle="${1:?укажи путь к локальному бандлу .tgz}"
[ -f "$bundle" ] || { echo "нет файла: $bundle" >&2; exit 1; }

base="$(basename "$bundle")"
ssh "$REMOTE" "mkdir -p $REMOTE_DIR/backups"
scp "$bundle" "$REMOTE:$REMOTE_DIR/backups/$base"
ssh -t "$REMOTE" "bash $REMOTE_DIR/scripts/restore.sh $REMOTE_DIR/backups/$base"
```

- [ ] **Step 5: Тест — статическая проверка всех скриптов**

Run:
```bash
cd catalog
for f in scripts/pull-backup.sh scripts/pull-env.sh scripts/push-restore.sh scripts/backup.sh scripts/restore.sh; do
  bash -n "$f" && echo "syntax ok: $f"
done
command -v shellcheck >/dev/null && shellcheck scripts/*.sh || echo "shellcheck не установлен — пропускаю"
```
Expected: `syntax ok` для каждого файла; shellcheck (если есть) без ошибок уровня error.

- [ ] **Step 6: Commit**

```bash
git add scripts/pull-backup.sh scripts/pull-env.sh scripts/push-restore.sh
git commit -m "feat(ops): add mac-side pull/push backup scripts"
```

---

### Task 4: `Makefile` + раздел в `README.md`

**Files:**
- Create: `catalog/Makefile`
- Modify: `catalog/README.md`

**Interfaces:**
- Consumes: скрипты из Task 1–3.
- Produces: цели `make pull`, `make pull-secrets`, `make restore FILE=…` (Mac) и `make backup` (сервер). Переменная `REMOTE ?= root@201.51.4.231`.

- [ ] **Step 1: Тест — `make` без Makefile падает**

Run:
```bash
cd catalog
make -n pull
```
Expected: FAIL — `make: *** No rule to make target 'pull'` или `No such file`.

- [ ] **Step 2: Написать `catalog/Makefile`**

```makefile
# Бэкапы каталога. Переопределить сервер: make pull REMOTE=root@1.2.3.4
REMOTE ?= root@201.51.4.231
export REMOTE

.PHONY: pull pull-secrets restore backup help

help:
	@echo "make pull          — Mac: бэкап на сервере + скачать сюда"
	@echo "make pull-secrets  — Mac: скачать .env (редко, при смене секретов)"
	@echo "make restore FILE=~/gena-lavki-backups/....tgz — Mac: залить бандл и восстановить"
	@echo "make backup        — Server: сделать бэкап локально на сервере (запускать НА VPS)"

pull:
	./scripts/pull-backup.sh

pull-secrets:
	./scripts/pull-env.sh

restore:
	@test -n "$(FILE)" || { echo "Укажи FILE=~/gena-lavki-backups/....tgz"; exit 1; }
	./scripts/push-restore.sh "$(FILE)"

backup:
	bash /opt/gena-lavki/scripts/backup.sh
```

- [ ] **Step 3: Тест — dry-run целей печатает правильные команды**

Run:
```bash
cd catalog
make -n pull
make -n pull-secrets
make -n restore FILE=/tmp/x.tgz
```
Expected: печатает соответственно `./scripts/pull-backup.sh`, `./scripts/pull-env.sh`, `./scripts/push-restore.sh /tmp/x.tgz` (без реального запуска).

- [ ] **Step 4: Добавить раздел в `catalog/README.md`**

Вставить перед разделом «## Полезные ссылки»:

````markdown
## Резервное копирование и восстановление

Ручные бэкапы БД (`catalog`) и загрузок (`public/uploads`). Копии живут на VPS
(быстрый откат) и скачиваются на Mac (off-site). Секреты (`.env`) бэкапятся отдельно.
Дизайн: `docs/superpowers/specs/2026-07-29-backups-design.md`.

### Регулярно (с Mac)

```bash
make pull          # сделать бэкап на сервере и скачать в ~/gena-lavki-backups/
```

Запускай перед каждым деплоем/правкой контента и раз в неделю.

```bash
make pull-secrets  # разово: скачать .env в ~/gena-lavki-backups/secrets/ (при смене секретов)
```

### Восстановление

**На том же VPS** (кривая миграция / удалили данные):

```bash
ssh root@201.51.4.231
bash /opt/gena-lavki/scripts/restore.sh /opt/gena-lavki/backups/gena-lavki_<ts>.tgz
```

**Из копии на Mac** (в т.ч. на новом сервере после пересборки):

```bash
make restore FILE=~/gena-lavki-backups/gena-lavki_<ts>.tgz
```

### Защита от удаления сервера за неоплату

Бэкапы — страховка. В панели Timeweb включи автоплатёж, держи буфер на балансе и
уведомления о низком балансе — это не даёт наступить 7-дневному удалению VPS.
````

- [ ] **Step 5: Commit**

```bash
git add Makefile README.md
git commit -m "feat(ops): add backup Makefile targets and README section"
```

---

### Task 5: Доставка серверных скриптов деплоем (`deploy.yml`)

**Files:**
- Modify: `catalog/.github/workflows/deploy.yml` (шаг scp)

**Interfaces:**
- Consumes: `scripts/backup.sh`, `scripts/restore.sh` из репозитория.
- Produces: эти файлы на сервере в `/opt/gena-lavki/scripts/` при каждом деплое (appleboy/scp-action сохраняет относительный путь `scripts/…`).

- [ ] **Step 1: Изменить `source` в шаге scp**

В `catalog/.github/workflows/deploy.yml` заменить строку шага «Copy compose and Caddy config to server»:

Было:
```yaml
      - name: Copy compose and Caddy config to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: "docker-compose.prod.yml,Caddyfile"
          target: /opt/gena-lavki
```

Стало:
```yaml
      - name: Copy compose, Caddy config and ops scripts to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: "docker-compose.prod.yml,Caddyfile,scripts/backup.sh,scripts/restore.sh"
          target: /opt/gena-lavki
```

- [ ] **Step 2: Тест — валидный YAML и наличие скриптов в source**

Run:
```bash
cd catalog
python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/deploy.yml')); print('yaml ok')"
grep -q 'scripts/backup.sh,scripts/restore.sh' .github/workflows/deploy.yml && echo "source updated"
```
Expected: `yaml ok` и `source updated`.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: ship backup/restore scripts to server on deploy"
```

- [ ] **Step 4: Приёмка на реальном сервере (после деплоя)**

После мёрджа в `main` и прохождения деплоя:
```bash
# скрипты доехали
ssh root@201.51.4.231 'ls -l /opt/gena-lavki/scripts/'
# полный цикл с Mac
make pull
ls -lt ~/gena-lavki-backups/ | head
make pull-secrets
ls -l ~/gena-lavki-backups/secrets/.env
```
Expected: на сервере есть `backup.sh`/`restore.sh`; `make pull` создаёт и качает бандл; `.env` скачан с правами `600`.

---

## Notes on execution

- Задачи 1–4 полностью тестируются локально на dev-стеке (`docker-compose.yml`) и на Mac — сервер не нужен.
- Задача 5 меняет CI; её финальная приёмка (Step 4) выполняется уже после деплоя, на живом сервере.
- Коммиты в плане — рекомендованный ритм; фактические коммиты делать по договорённости с пользователем (репозиторий — `catalog/`, не мёржить в `main` без явной отмашки).
